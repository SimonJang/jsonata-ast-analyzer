---
phase: 10-business-rule-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/business-rules.test.ts
autonomous: true
requirements:
  - BIZR-01
  - BIZR-02
  - BIZR-03
  - BIZR-04
  - BIZR-05

must_haves:
  truths:
    - "Conditional expressions (ternary, elvis ?:, coalescing ??) extract paths from all branches in passing tests"
    - "Compound boolean filter predicates (and/or) extract paths for every referenced field in passing tests"
    - "Aggregation functions ($sum, $count, $average) over nested arrays extract the correct nested path in passing tests"
    - "Lookup and cross-reference patterns extract both object and key paths in passing tests"
    - "Variable-driven object construction resolves all variable references back to source paths in passing tests"
    - "Known analyzer bugs are documented as it.skip tests with BUG(v1.2) tracking comments"
    - "All tests use assertFixture() with exact match mode and explicit confidence in every PathResult"
  artifacts:
    - path: "test/integration/business-rules.test.ts"
      provides: "All BIZR-01 through BIZR-05 integration test fixtures plus composite"
      contains: "BIZR-01|BIZR-02|BIZR-03|BIZR-04|BIZR-05|Composite"
      min_lines: 200
  key_links:
    - from: "test/integration/business-rules.test.ts"
      to: "test/integration/helpers.ts"
      via: "import { assertFixture } and IntegrationFixture"
      pattern: "import.*assertFixture.*helpers"
    - from: "test/integration/business-rules.test.ts"
      to: "src/index.ts"
      via: "assertFixture calls extractPaths internally"
      pattern: "assertFixture"
---

<objective>
Write integration test fixtures for all five BIZR requirements (BIZR-01 through BIZR-05) in the existing `business-rules.test.ts` skeleton, validating path extraction from conditional logic, compound filters, aggregation, lookups, and variable-driven object construction.

Purpose: Validate that the path analyzer correctly extracts paths from business rule patterns -- the second most common JSONata production use case after data transforms. Document known bugs via `it.skip` for v1.2.

Output: A fully populated `test/integration/business-rules.test.ts` with ~18-22 fixtures organized into 5 nested describe blocks plus a composite, all using `assertFixture()` with exact match and explicit confidence.
</objective>

<execution_context>
@/home/simon-mainframe/.claude/get-shit-done/workflows/execute-plan.md
@/home/simon-mainframe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-business-rule-tests/10-CONTEXT.md
@.planning/phases/10-business-rule-tests/10-RESEARCH.md

@test/integration/helpers.ts
@test/integration/business-rules.test.ts
@test/integration/data-transforms.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From src/types.ts:
```typescript
export type Confidence = "static" | "dynamic" | "partial";
export interface PathResult {
  path: string;
  confidence: Confidence;
}
```

From src/index.ts:
```typescript
export function extractPaths(expression: string): PathResult[];
export type { PathResult } from "./types.js";
export type { Confidence } from "./types.js";
```

From test/integration/helpers.ts:
```typescript
interface ExactFixture extends FixtureBase {
  expectedPaths: PathResult[];
  mustContain?: never;
  mustNotContain?: never;
}
export type IntegrationFixture = ExactFixture | SubsetFixture;
export function sortPaths(paths: PathResult[]): PathResult[];
export function assertFixture(fixture: IntegrationFixture): void;
```
</interfaces>

<verified_expressions>
<!-- CRITICAL: These expression -> output mappings were verified by running extractPaths() live during research.
     The executor MUST still pre-check each expression before writing the fixture (per CONTEXT.md decision),
     but these serve as known-good starting points. -->

BIZR-01 (all passing -- no bugs found):
  - `status = "active" ? order.total : order.estimate` => order.estimate, order.total, status (all static)
  - `type = "A" ? price.retail : type = "B" ? price.wholesale : price.default` => price.default, price.retail, price.wholesale, type (all static)
  - `nickname ?? firstName` => firstName, nickname (all static)
  - `customer.email ?: customer.phone` => customer.email, customer.phone (all static)

BIZR-02 (all passing when no variables in predicates):
  - `orders[status = "active" and total > 100].id` => orders.id, orders.status, orders.total (all static)
  - `products[(active and inStock) or featured].title` => products.active, products.featured, products.inStock, products.title (all static)

BIZR-03 (all passing):
  - `$sum(orders.items.price)` => orders.items.price (static)
  - `$sum($map(orders.items, function($v) { $v.price * $v.qty }))` => orders.items, orders.items.price, orders.items.qty (all static)

BIZR-04 (some passing, some buggy):
  - `$lookup(products, orders.productId)` => orders.productId, products (all static) [PASSING]
  - `($config := settings; order.amount * $config.taxRate)` => order.amount, settings, settings.taxRate (all static) [PASSING]
  - `$lookup(obj, key).field` => BUGGY -- function args lost (EDGE-05 tech debt) [SKIP]
  - variable-resolved paths inside filter predicates => BUGGY -- spurious context-prefixed paths [SKIP]

BIZR-05 (all passing):
  - `($order := order; {"id": $order.id, "total": $order.total, "customer": $order.customer.name})` => order, order.customer.name, order.id, order.total (all static)
  - `($cust := customer; $addr := $cust.address; {"name": $cust.name, "city": $addr.city, "zip": $addr.zip})` => customer, customer.address, customer.address.city, customer.address.zip, customer.name (all static)
</verified_expressions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write BIZR-01, BIZR-02, and BIZR-03 fixtures (conditionals, compound filters, aggregation)</name>
  <files>test/integration/business-rules.test.ts</files>
  <action>
Replace the entire contents of `business-rules.test.ts` with the new test structure. Add imports (`describe`, `it` from vitest; `assertFixture` and `IntegrationFixture` from helpers). Structure the file as:

```typescript
import { describe, it } from "vitest";
import { assertFixture } from "./helpers.js";
import type { IntegrationFixture } from "./helpers.js";

describe("Business Rules", () => {
  describe("BIZR-01: ...", () => { ... });
  describe("BIZR-02: ...", () => { ... });
  describe("BIZR-03: ...", () => { ... });
  // BIZR-04, BIZR-05, Composite -- Task 2 fills these
});
```

**BIZR-01 fixtures (4 passing, 0 skipped -- research found no bugs):**

All four conditional forms work correctly. Write these fixtures:

1. Simple ternary: `status = "active" ? order.total : order.estimate`
   Expected: order.estimate, order.total, status (all static)

2. Nested ternary: `type = "A" ? price.retail : type = "B" ? price.wholesale : price.default`
   Expected: price.default, price.retail, price.wholesale, type (all static)

3. Null coalescing (??): `nickname ?? firstName`
   Expected: firstName, nickname (all static)

4. Elvis operator (?:): `customer.email ?: customer.phone`
   Expected: customer.email, customer.phone (all static)

Fixture names follow the pattern: `"simple ternary: extracts condition and both branch paths"`, `"nested ternary: extracts condition paths and all branch paths"`, `"null coalescing: extracts both fallback paths"`, `"elvis operator: extracts source and fallback paths"`.

**BIZR-02 fixtures (3 passing, 0 skipped -- research found no bugs for literal-only predicates):**

1. AND filter: `orders[status = "active" and total > 100].id`
   Expected: orders.id, orders.status, orders.total (all static)

2. Mixed AND-OR: `products[(active and inStock) or featured].title`
   Expected: products.active, products.featured, products.inStock, products.title (all static)

3. Multi-field OR: pre-check an expression like `employees[department = "eng" or department = "design"].name` or `items[price > 50 or rating >= 4].name` -- a different shape from #1 and #2.

**BIZR-03 fixtures (3-4 passing, 0 skipped -- research found no bugs):**

1. Sum over nested path: `$sum(orders.items.price)`
   Expected: orders.items.price (static)

2. Sum with map lambda: `$sum($map(orders.items, function($v) { $v.price * $v.qty }))`
   Expected: orders.items, orders.items.price, orders.items.qty (all static)

3. Count with filtered array: pre-check `$count(orders[status = "active"])` -- should produce orders, orders.status (the base array path AND the filter predicate path per Pitfall 5 in research).

4. (Optional) Average or min/max over nested path: pre-check something like `$average(reviews.score)` or `$min(products.price)`.

**Pattern rules (from CONTEXT.md locked decisions):**
- Use `IntegrationFixture[]` array + `for...of` loop + `assertFixture()` for passing fixtures
- Use standalone `it.skip(name, () => { assertFixture({...}) })` for skipped fixtures, OUTSIDE the fixture array
- Multi-line template literals for complex expressions; single-line strings for simple ones
- Realistic variable names: `$order`, `$customer`, `$config` (not `$a`, `$b`)
- Fixture names combine pattern and behavior: `"AND filter predicate: extracts all field paths from compound condition"`
- Confidence level explicit in EVERY PathResult
- No inline JSONata comments

**CRITICAL: Pre-check every expression through extractPaths() before writing the fixture.** The research provides verified patterns but the executor MUST still confirm the actual output for any new expressions not listed in the verified_expressions block above. Use `npx vitest run test/integration/business-rules.test.ts` to validate iteratively.

Leave empty `describe` blocks for BIZR-04, BIZR-05, and Composite as placeholders for Task 2.
  </action>
  <verify>
    <automated>npx vitest run test/integration/business-rules.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - BIZR-01 describe block has 4 passing fixtures covering ternary, nested ternary, coalescing, and elvis
    - BIZR-02 describe block has 3 passing fixtures covering AND, OR, and mixed compound predicates
    - BIZR-03 describe block has 3-4 passing fixtures covering $sum, $count, and aggregation with map lambda
    - All fixtures use assertFixture() with exact match (expectedPaths) and explicit confidence
    - `npx vitest run test/integration/business-rules.test.ts` passes with 0 failures
  </done>
</task>

<task type="auto">
  <name>Task 2: Write BIZR-04, BIZR-05, and Composite fixtures (lookups, variable construction, composite)</name>
  <files>test/integration/business-rules.test.ts</files>
  <action>
Fill in the BIZR-04, BIZR-05, and Composite describe blocks that Task 1 left as placeholders.

**BIZR-04 fixtures (2-3 passing + 1-2 skipped):**

Passing fixtures -- use expressions verified in research that avoid known bugs:
1. Direct $lookup: `$lookup(products, orders.productId)`
   Expected: orders.productId, products (all static)

2. Variable cross-reference (clean, no filter): `($config := settings; order.amount * $config.taxRate)`
   Expected: order.amount, settings, settings.taxRate (all static)

3. (Optional) Another clean cross-reference pattern, e.g., a map-based lookup where one array's values reference another structure via variable binding (pre-check first!)

Skipped fixtures -- document known bugs:
1. `it.skip` for $lookup result chaining (EDGE-05): Use `$lookup(products, sku).price` or similar.
   Comment: `// BUG(v1.2): $lookup(obj, key).field loses function arguments -- only .field appears (EDGE-05 tech debt)`
   Show CORRECT expected output in the fixture.

2. `it.skip` for variable-in-filter cross-reference: Use something like `($min := minPrice; products[price >= $min].name)`.
   Comment: `// BUG(v1.2): variable-resolved paths in filter predicates get spuriously context-prefixed (filter predicate path leak)`
   Show CORRECT expected output in the fixture.

**BIZR-05 fixtures (3-4 passing, 0 skipped -- research found no bugs for non-filter patterns):**

1. Single variable object construction: `($order := order; {"id": $order.id, "total": $order.total, "customer": $order.customer.name})`
   Expected: order, order.customer.name, order.id, order.total (all static)

2. Multi-variable two-hop chain: `($cust := customer; $addr := $cust.address; {"name": $cust.name, "city": $addr.city, "zip": $addr.zip})`
   Expected: customer, customer.address, customer.address.city, customer.address.zip, customer.name (all static)

3. Multi-source object: pre-check an expression like `($o := order; $c := customer; {"orderId": $o.id, "customerName": $c.name, "amount": $o.total})` -- binds from TWO separate root-level sources and builds an object (pre-check first!)

4. (Optional) Variable with aggregation in object: pre-check something like `($items := order.lineItems; {"count": $count($items), "total": $sum($map($items, function($v) { $v.price }))})` -- variable combined with aggregation functions inside an object constructor (pre-check first!)

**Composite fixture (1 passing):**

After all five BIZR describe blocks, add:

```typescript
describe("Composite: cross-pattern business rule", () => {
  const fixtures: IntegrationFixture[] = [
    {
      name: "combined conditional + aggregation + variable: resolves all paths across BIZR-01/03/05 patterns",
      expression: `...`,
      expectedPaths: [ /* ... */ ],
    },
  ];

  for (const fixture of fixtures) {
    it(fixture.name, () => {
      assertFixture(fixture);
    });
  }
});
```

Choose a composite expression that combines clean (non-buggy) patterns from at least 2-3 BIZR categories. Good candidate: a block expression that uses a ternary conditional (BIZR-01) to select a path, feeds it into an aggregation (BIZR-03), and assigns the result via variable binding into an object (BIZR-05). Avoid patterns with variables in filter predicates or $lookup.field chaining.

Pre-check the composite expression carefully -- it MUST pass without `it.skip`.

**Same pattern rules as Task 1:** IntegrationFixture[] array, for...of loop, assertFixture(), realistic names, explicit confidence, BUG(v1.2) tracking comments for skipped tests.

**CRITICAL: Pre-check every expression.** Any expression not in the verified_expressions list MUST be tested via extractPaths() before being written as a fixture. Run `npx vitest run test/integration/business-rules.test.ts` to validate iteratively.
  </action>
  <verify>
    <automated>npx vitest run test/integration/business-rules.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    - BIZR-04 describe block has 2-3 passing fixtures and 1-2 it.skip fixtures with BUG(v1.2) tracking comments
    - BIZR-05 describe block has 3-4 passing fixtures covering variable binding and object construction
    - Composite describe block has 1 passing fixture combining multiple BIZR patterns
    - All passing tests use assertFixture() with exact match and explicit confidence
    - All skipped tests have `// BUG(v1.2): ...` tracking comments and show CORRECT expected output
    - `npx vitest run test/integration/business-rules.test.ts` passes with 0 failures
    - `npm run test:integration` passes (all integration test files still work)
    - `npm test` passes with zero failures
    - Total: ~18-22 fixtures across all BIZR blocks (15+ passing, 2-3 skipped)
    - No `mustContain` usage anywhere -- exact match only per CONTEXT.md decision
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run test/integration/business-rules.test.ts` -- all non-skipped tests pass, skipped tests reported
2. `npm run test:integration` -- full integration suite passes (including data-transforms and other category files)
3. `npm run test:unit` -- existing unit tests unaffected
4. `npm test` -- full suite passes
5. Grep for describe blocks: every BIZR-01 through BIZR-05 has its own describe block
6. Grep for `Composite`: composite describe block exists
7. Grep for `BUG(v1.2)`: all skipped tests have consistent tracking comments
8. Grep for `it.skip`: all skipped tests still contain assertFixture call with expectedPaths showing the CORRECT expected output
9. No `mustContain` usage anywhere -- exact match only per CONTEXT.md decision
</verification>

<success_criteria>
- test/integration/business-rules.test.ts contains 5 nested describe blocks (BIZR-01 through BIZR-05) plus a composite describe
- 15+ passing test fixtures across all blocks
- 2-3 skipped test fixtures documenting known analyzer bugs with BUG(v1.2) tracking comments
- Every fixture uses assertFixture() with exact match (expectedPaths) and explicit confidence in every PathResult
- `npm test` passes with zero failures (skipped tests do not count as failures)
- No subset matching (mustContain/mustNotContain) used anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/10-business-rule-tests/10-01-SUMMARY.md`
</output>
