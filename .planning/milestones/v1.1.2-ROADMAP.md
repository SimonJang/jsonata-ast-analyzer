# Milestone v1.1.2: CI Fix

**Status:** SHIPPED 2026-03-06
**Phases:** 17
**Total Plans:** 1

## Overview

Fix CI pipeline so all 294 tests pass by adding a build step before the test run. The CI workflow was missing a `pnpm build` step, causing CLI round-trip tests to fail because `dist/cli.js` didn't exist.

## Phases

### Phase 17: CI Build Step

**Goal**: CI pipeline produces build artifacts before running tests so all 294 tests pass including CLI round-trip tests
**Depends on**: Nothing (standalone CI config change)
**Plans**: 1 plan

Plans:
- [x] 17-01: Add build step to CI workflow

**Details:**
- Inserted `pnpm build` step in `.github/workflows/ci.yml` after typecheck and before test
- Single-line insertion with no step names or comments -- matches existing workflow style
- Verified full CI sequence locally: typecheck, build, test all pass

---

## Milestone Summary

**Key Decisions:**
- Decision: Single-line insertion approach (Rationale: Minimal diff, matches existing CI workflow style)
- Decision: Build step between typecheck and test (Rationale: Typecheck validates types first, build produces artifacts, test runs against built output)

**Issues Resolved:**
- CI pipeline failing on CLI round-trip tests because `dist/cli.js` was not built

**Issues Deferred:**
None.

**Technical Debt Incurred:**
None.

---

_For current project status, see .planning/ROADMAP.md_
