---
phase: 22-architecture-and-limitations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [ARCH-01, ARCH-02, LMTS-01]

must_haves:
  truths:
    - "README contains a How It Works section with a compact ASCII pipeline diagram showing expression string through parse, walk, dedupe, classify to PathResult[]"
    - "README explains the over-approximation design principle as a paragraph at the end of How It Works, including WHY false positives are safe and false negatives are dangerous"
    - "README documents exactly three limitations framed as deliberate design decisions with rationale, not as deficiencies"
  artifacts:
    - path: "README.md"
      provides: "How It Works and Limitations content"
      contains: "expression string"
  key_links:
    - from: "How It Works section"
      to: "Limitations section"
      via: "Over-approximation concept bridges architecture explanation to limitation framing"
      pattern: "over-approximat"
---

<objective>
Write the "How It Works" and "Limitations" content for the README, completing the final phase of the v1.1.3 documentation milestone.

Purpose: A developer evaluating the tool can understand how the analyzer works internally (pipeline stages, over-approximation principle) and what it intentionally does not do (three limitations framed as design decisions).
Output: README.md updated with prose content under the existing empty `## How It Works` and `## Limitations` headings.
</objective>

<execution_context>
@/home/simon-mainframe/.claude/get-shit-done/workflows/execute-plan.md
@/home/simon-mainframe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-architecture-and-limitations/22-CONTEXT.md
@.planning/phases/22-architecture-and-limitations/22-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write How It Works section (pipeline diagram + stage descriptions + over-approximation)</name>
  <files>README.md</files>
  <action>
Insert content under the existing `## How It Works` heading (currently line 173 of README.md, empty). Content goes BETWEEN `## How It Works` and `## Limitations`. Do NOT modify any other sections.

Write the following in order:

1. **ASCII pipeline diagram** -- compact single-line flow style (NOT box diagram). Stages in order:
   - expression string -> parse -> walk -> dedupe -> classify -> PathResult[]
   - Each stage gets a brief conceptual label below it (e.g., "JSONata parser -> AST", "recursive traversal -> raw paths", "Set-based dedup", "path string -> confidence")
   - Top-level stages ONLY -- no scope/variable resolution detail in the diagram
   - Conceptual labels ONLY -- do NOT mention source file names (parser.ts, walker.ts, etc.)

2. **Prose description of stages** (a few sentences per stage, covering the 4 main stages):
   - **Parse**: Delegates to the official JSONata parser (the `jsonata` package) to produce an AST. The analyzer does not reimplement parsing.
   - **Walk**: Recursively traverses the AST, tracking variable assignments across scopes to resolve where data actually comes from. Briefly mention that it handles variable bindings, filter predicates, and function arguments (these are the core value proposition). Do NOT explain implementation details like linked-list scope chains or two-pass filter algorithms.
   - **Dedupe**: Removes duplicate paths using Set-based deduplication.
   - **Classify**: Annotates each unique path with a confidence level -- `static` (fully resolved), `dynamic` (contains `[*]` wildcard), or `partial` (contains `%` parent marker).

3. **Over-approximation paragraph** -- placed at the END of How It Works, right before the Limitations heading. This is a standalone paragraph (NOT a separate subsection, NOT under a subheading). Must include:
   - The analyzer reports a superset of paths that may be accessed (both branches of conditionals are walked, unresolvable variables produce wildcards rather than being omitted)
   - One sentence explaining WHY: false positives (extra paths reported) are safe for downstream consumers; false negatives (missed paths) could break data dependency tracking

Anti-patterns to AVOID:
- Do NOT use "Architecture" as a heading -- keep existing "How It Works"
- Do NOT mention source file names (parser.ts, walker.ts, etc.) -- conceptual labels only per user decision
- Do NOT create a separate subsection for over-approximation -- it is a paragraph within How It Works
- Do NOT include test suite size, validation metrics, or contributor information
- Do NOT add a Mermaid diagram -- ASCII only
- Do NOT over-explain internal details (scope chains, thunk unwrapping, filter stage algorithm)
- Do NOT add concrete code examples of superset behavior -- the concept is clear in prose, and Phase 21 examples already demonstrate it
- Do NOT use analogy to other static analyzers -- keep it direct, audience understands static analysis

Style: Match the established README style from phases 18-21: clean, minimal prose, no badges, no TOC.
  </action>
  <verify>
    <automated>grep -c "expression string" README.md | grep -q "[1-9]" && grep -c "over-approximat" README.md | grep -q "[1-9]" && grep -c "parse" README.md | grep -q "[1-9]" && echo "PASS: How It Works content present" || echo "FAIL: Missing How It Works content"</automated>
  </verify>
  <done>README contains: (1) ASCII pipeline diagram with all 5 stages and brief labels, (2) prose descriptions of parse, walk, dedupe, classify stages mentioning JSONata parser delegation, variable tracing, and confidence classification, (3) over-approximation paragraph at end of section explaining superset reporting and WHY false positives beat false negatives</done>
</task>

<task type="auto">
  <name>Task 2: Write Limitations section (three design decisions)</name>
  <files>README.md</files>
  <action>
Insert content under the existing `## Limitations` heading (currently line 175 of README.md, empty). Content goes BETWEEN `## Limitations` and `## License`. Do NOT modify any other sections.

Write exactly three limitation items. Each item uses the format: **bold heading** followed by one sentence of rationale. The items are:

1. **Static analysis only** -- The analyzer works from the expression text alone, without evaluating it or requiring sample input data. This means it cannot resolve values that depend on runtime state, but it can run anywhere without a live environment.

2. **Dynamic path wildcards** -- When a variable in bracket position cannot be statically resolved, the analyzer emits a `[*]` wildcard segment and marks the path as `dynamic`. This acknowledges that a field is accessed while signaling that the exact field name depends on runtime data.

3. **Parent operator approximation** -- The parent operator (`%`) navigates to an enclosing context that is only fully determined at runtime. The analyzer preserves `%` as a literal path segment and marks the result as `partial`, recording the structural relationship without claiming to know the exact target.

Framing rules (per user decision):
- Frame as DELIBERATE DESIGN DECISIONS with rationale, NOT as deficiencies or apologies
- Use active voice: "The analyzer does X because Y" -- NOT "Unfortunately the analyzer cannot..."
- Do NOT include concrete code examples -- the Examples section already covers these patterns
- Do NOT add a fourth limitation -- exactly three items, with "static analysis only" and "no evaluation" merged into item 1
- Each item is bold heading + one sentence of rationale (can be 1-2 sentences for clarity, but keep concise)

Style: Match the established README style. Clean, direct prose.
  </action>
  <verify>
    <automated>grep -c "Static analysis only" README.md | grep -q "[1-9]" && grep -c "Dynamic path wildcards" README.md | grep -q "[1-9]" && grep -c "Parent operator approximation" README.md | grep -q "[1-9]" && echo "PASS: All 3 limitations present" || echo "FAIL: Missing limitations"</automated>
  </verify>
  <done>README Limitations section contains exactly three items: (1) Static analysis only -- framed as working without runtime/sample data, (2) Dynamic path wildcards -- framed as acknowledging access while signaling runtime dependency, (3) Parent operator approximation -- framed as preserving structure while noting runtime-only resolution. All framed as design decisions with rationale, not deficiencies.</done>
</task>

</tasks>

<verification>
1. `pnpm test` passes (no existing tests broken by README edits)
2. README section order preserved: ... Examples -> How It Works -> Limitations -> License
3. No source file names appear in How It Works section (grep for ".ts" in the How It Works block)
4. Over-approximation is a paragraph within How It Works, not a separate subsection
5. Exactly three limitation items, no more
</verification>

<success_criteria>
- README How It Works section contains ASCII pipeline diagram, stage descriptions, and over-approximation paragraph
- README Limitations section contains exactly three design-decision-framed limitation items
- `pnpm test` remains green
- All three requirements (ARCH-01, ARCH-02, LMTS-01) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/22-architecture-and-limitations/22-01-SUMMARY.md`
</output>
