---
phase: 20-cli-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]

must_haves:
  truths:
    - "README shows jsonata-paths argument mode with a runnable example and output"
    - "README shows jsonata-paths stdin/pipe mode with a runnable example and output"
    - "README includes a shell quoting note explaining single vs double quotes for $ in expressions"
  artifacts:
    - path: "README.md"
      provides: "CLI Usage documentation with argument mode, stdin mode, quoting note, and error behavior"
      contains: "### Argument Mode"
    - path: "README.md"
      provides: "Stdin mode documentation"
      contains: "### Stdin Mode"
  key_links:
    - from: "README.md ## CLI Usage"
      to: "README.md ## Examples"
      via: "Content inserted between existing headings without duplicating or deleting either"
      pattern: "## CLI Usage[\\s\\S]+### Argument Mode[\\s\\S]+### Stdin Mode[\\s\\S]+## Examples"
---

<objective>
Add CLI documentation to README.md under the existing empty `## CLI Usage` heading.

Purpose: Enable developers to use `jsonata-paths` from the command line for both one-off queries and piped workflows, and avoid the silent shell quoting pitfall with `$` expressions.

Output: README.md with complete CLI Usage section containing Argument Mode, Stdin Mode, shell quoting note, and error behavior sentence.
</objective>

<execution_context>
@/home/simon-mainframe/.claude/get-shit-done/workflows/execute-plan.md
@/home/simon-mainframe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cli-documentation/20-CONTEXT.md
@.planning/phases/20-cli-documentation/20-RESEARCH.md

@README.md

<interfaces>
<!-- No code interfaces -- this is a documentation-only plan. -->
<!-- Key context: README.md line 67 has `## CLI Usage` (empty), line 69 has `## Examples`. -->
<!-- Content must be inserted between these two headings. Do NOT duplicate either heading. -->

Current README structure (lines 67-69):
```
## CLI Usage

## Examples
```

Established formatting pattern from Installation section:
```markdown
> **Note:** This package is ESM-only. Use `import` -- `require()` is not supported.
```

Verified CLI outputs (from RESEARCH.md):
- Argument mode: `jsonata-paths 'account.name'` produces `[{"path":"account.name","confidence":"static"}]`
- Stdin mode: `echo '$sum(orders.total)' | jsonata-paths` produces `[{"path":"orders.total","confidence":"static"}]`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CLI Usage content in README.md</name>
  <files>README.md</files>
  <action>
Insert CLI documentation content between the existing `## CLI Usage` heading (line 67) and `## Examples` heading (line 69) in README.md. Do NOT duplicate or remove either heading.

Structure to insert (after line 67, before `## Examples`):

1. `### Argument Mode` subsection:
   - Brief intro sentence (Claude's discretion on wording)
   - Code block (sh) showing: `jsonata-paths 'account.name'`
   - Code block (json) showing output: `[{"path":"account.name","confidence":"static"}]`

2. `### Stdin Mode` subsection:
   - Brief intro sentence (Claude's discretion on wording)
   - Code block (sh) showing: `echo '$sum(orders.total)' | jsonata-paths`
   - Code block (json) showing output: `[{"path":"orders.total","confidence":"static"}]`

3. Shell quoting note as blockquote callout (immediately after Stdin Mode):
   - Use `> **Note:**` format matching the ESM notice in Installation
   - Explain that `$` in JSONata expressions requires single quotes to prevent shell variable expansion
   - Include embedded code block with `# Correct` and `# Wrong` comments using `$sum(prices)` as the example expression per CONTEXT.md
   - Mention that double quotes cause bash to expand `$sum` to an empty string

4. Error behavior (after the quoting note):
   - One sentence: invalid expressions exit with code 1 and print the error to stderr
   - No error output example per CONTEXT.md

Key constraints:
- Argument Mode example MUST use `account.name` (different from Quick Example's filter expression) per CONTEXT.md
- Stdin Mode example MUST use `echo '$sum(orders.total)' | jsonata-paths` per CONTEXT.md
- Output MUST be compact single-line JSON (matches actual CLI output from JSON.stringify)
- Quoting note MUST use `$sum(prices)` as the correct/wrong comparison expression per CONTEXT.md
- No usage/help output block at the top per CONTEXT.md
- No file piping examples (cat) per CONTEXT.md
- Unix-only -- no Windows/PowerShell mention per CONTEXT.md
- Do NOT add `## CLI Usage` heading (it already exists)
- Do NOT delete or modify the `## Examples` heading below
  </action>
  <verify>
    <automated>grep -c "### Argument Mode" README.md && grep -c "### Stdin Mode" README.md && grep -c "# Correct" README.md && grep -c "# Wrong" README.md && test $(grep -c "## CLI Usage" README.md) -eq 1 && echo "All checks passed"</automated>
  </verify>
  <done>
- README.md contains `### Argument Mode` with `jsonata-paths 'account.name'` command and compact JSON output
- README.md contains `### Stdin Mode` with `echo '$sum(orders.total)' | jsonata-paths` command and compact JSON output
- README.md contains `> **Note:**` quoting callout with `# Correct` / `# Wrong` comparison using `$sum(prices)`
- README.md contains one-sentence error behavior mention
- `## CLI Usage` heading appears exactly once (not duplicated)
- `## Examples` heading still present and unmodified
- `pnpm test` passes (no regressions from doc-only change)
  </done>
</task>

</tasks>

<verification>
1. Structural checks: `### Argument Mode` and `### Stdin Mode` headings exist under `## CLI Usage`
2. Content checks: Quoting note contains `# Correct` and `# Wrong` markers
3. Heading integrity: `## CLI Usage` appears exactly once, `## Examples` still present
4. No regressions: `pnpm test` passes
5. Section order preserved: CLI Usage appears before Examples in README
</verification>

<success_criteria>
- README.md CLI Usage section contains argument mode, stdin mode, quoting note, and error behavior
- All three requirement IDs (CLI-01, CLI-02, CLI-03) are satisfied
- README section structure is intact (no duplicate or missing headings)
- Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-cli-documentation/20-01-SUMMARY.md`
</output>
