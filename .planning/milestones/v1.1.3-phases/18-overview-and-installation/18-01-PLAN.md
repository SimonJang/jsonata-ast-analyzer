---
phase: 18-overview-and-installation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - package.json
autonomous: true
requirements:
  - OVVW-01
  - OVVW-02
  - INST-01
  - INST-02
  - LIC-01

must_haves:
  truths:
    - "A developer landing on the README immediately understands the library extracts data paths from JSONata expressions"
    - "A developer sees a concrete expression-in/paths-out example before being asked to install anything"
    - "A developer can copy a pnpm, npm, or yarn install command and knows the package is ESM-only"
    - "A developer can see the license at the bottom of the README"
    - "Empty headings exist for API Reference, CLI Usage, Examples, How It Works, and Limitations -- ready for Phases 19-22"
  artifacts:
    - path: "README.md"
      provides: "Complete overview, quick example, installation, skeleton headings, license"
      contains: "# jsonata-ast-analyzer"
    - path: "package.json"
      provides: "Corrected license field matching LICENSE file"
      contains: '"license": "MIT"'
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "Package name in install commands matches package.json name field"
      pattern: "jsonata-ast-analyzer"
    - from: "README.md"
      to: "LICENSE"
      via: "License section says MIT, matching LICENSE file content"
      pattern: "MIT"
---

<objective>
Replace the placeholder README with a structured document that opens with a value proposition and quick example, provides installation instructions with ESM-only notice, creates empty skeleton headings for Phases 19-22, and closes with a license section. Also fix the package.json license field from ISC to MIT to match the LICENSE file.

Purpose: A developer landing on the repository or npm page immediately understands what the library does, sees proof of its value, and can install it.
Output: Complete README.md with Phase 18 content written and Phase 19-22 headings as empty placeholders. Corrected package.json license field.
</objective>

<execution_context>
@/home/simon-mainframe/.claude/get-shit-done/workflows/execute-plan.md
@/home/simon-mainframe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-overview-and-installation/18-CONTEXT.md
@.planning/phases/18-overview-and-installation/18-RESEARCH.md

@README.md
@package.json
@LICENSE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix package.json license field</name>
  <files>package.json</files>
  <action>
Change the `license` field in package.json from `"ISC"` to `"MIT"` to match the LICENSE file which contains MIT License text (Copyright (c) 2025 Simon Jang). This is a one-line fix. Do not change any other fields.

Reason: The LICENSE file is the canonical legal document and contains MIT text. The package.json field was never updated from its default ISC value. The README license section (Task 2) will say MIT, so this must be consistent.
  </action>
  <verify>
    <automated>grep '"license": "MIT"' package.json</automated>
  </verify>
  <done>package.json license field reads "MIT", matching the LICENSE file</done>
</task>

<task type="auto">
  <name>Task 2: Write README with overview, example, installation, skeleton, and license</name>
  <files>README.md</files>
  <action>
Replace the entire contents of README.md with a new document following the locked section ordering from CONTEXT.md. The README must contain these sections in this exact order:

**1. Title (H1):** `# jsonata-ast-analyzer`

**2. One-liner description:** Immediately below the title (not in the heading itself). A single sentence, ~15-20 words max, stating what the library does and who it's for. No implementation jargon (no "AST walker", no "recursive descent"). Claude's discretion on exact wording. Target message: "Static analysis tool that tells you which data fields a JSONata expression will read."

**3. Quick Example (H2):** `## Quick Example`
Use the verified example from RESEARCH.md. Show a JavaScript code block with:
```javascript
import { extractPaths } from "jsonata-ast-analyzer";

const paths = extractPaths('orders[status = "active"].items.price');
// [
//   { path: "orders.items.price",  confidence: "static" },
//   { path: "orders.status",       confidence: "static" }
// ]
```
This example was verified against the built dist. Do NOT modify the output -- use it exactly as shown. This example demonstrates that the analyzer finds the hidden `orders.status` path inside the filter predicate.

**4. Installation (H2):** `## Installation`
Show three separate `sh` code blocks, one per package manager, with pnpm first (project uses pnpm):
```sh
pnpm add jsonata-ast-analyzer
```
```sh
npm install jsonata-ast-analyzer
```
```sh
yarn add jsonata-ast-analyzer
```

Immediately after the install commands, add an ESM-only notice as a blockquote:
```
> **Note:** This package is ESM-only. Use `import` -- `require()` is not supported.
```

**5. Empty skeleton headings:** Add these H2 headings with NO content below them (no placeholder text, no HTML comments, no "coming soon"). Just the heading followed by a blank line before the next heading:
- `## API Reference`
- `## CLI Usage`
- `## Examples`
- `## How It Works`
- `## Limitations`

Per user decision: "Empty headings only -- no placeholder text, no HTML comments, no 'coming soon' markers."

**6. License (H2):** `## License`
Single word: `MIT`
(The full text is in the LICENSE file. Keep the section minimal per research recommendation.)

**Explicit exclusions (per user decisions):**
- No badges row
- No table of contents
- No placeholder text in empty headings
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const content = fs.readFileSync('README.md', 'utf8');
const checks = [
  ['H1 title', /^# jsonata-ast-analyzer$/m],
  ['Quick Example heading', /^## Quick Example$/m],
  ['extractPaths import', /import.*extractPaths.*from.*jsonata-ast-analyzer/],
  ['Installation heading', /^## Installation$/m],
  ['pnpm install', /pnpm add jsonata-ast-analyzer/],
  ['npm install', /npm install jsonata-ast-analyzer/],
  ['yarn install', /yarn add jsonata-ast-analyzer/],
  ['ESM notice', /ESM-only/],
  ['API Reference heading', /^## API Reference$/m],
  ['CLI Usage heading', /^## CLI Usage$/m],
  ['Examples heading', /^## Examples$/m],
  ['How It Works heading', /^## How It Works$/m],
  ['Limitations heading', /^## Limitations$/m],
  ['License heading', /^## License$/m],
  ['MIT license', /MIT/],
  ['No badges', () => !content.includes('![')],
  ['Quick Example before Installation', () => content.indexOf('## Quick Example') < content.indexOf('## Installation')],
];
let pass = 0, fail = 0;
for (const [name, check] of checks) {
  const ok = typeof check === 'function' ? check() : check.test(content);
  if (ok) { pass++; } else { fail++; console.log('FAIL:', name); }
}
console.log(pass + '/' + checks.length + ' checks passed');
if (fail > 0) process.exit(1);
"</automated>
  </verify>
  <done>README.md contains: H1 title "jsonata-ast-analyzer", one-liner description, Quick Example with verified extractPaths output (before Installation), Installation with pnpm/npm/yarn commands and ESM-only notice, five empty skeleton headings (API Reference, CLI Usage, Examples, How It Works, Limitations), and License section saying MIT. No badges, no TOC, no placeholder text in empty headings.</done>
</task>

</tasks>

<verification>
1. `grep '"license": "MIT"' package.json` -- confirms license field fix
2. README.md structure verification script (in Task 2 verify) -- confirms all 17 structural checks
3. `pnpm test` -- confirms no code was accidentally broken (documentation-only change should not affect tests)
4. Visual review: README section order is title -> one-liner -> Quick Example -> Installation -> API Reference -> CLI Usage -> Examples -> How It Works -> Limitations -> License
</verification>

<success_criteria>
- README.md opens with `# jsonata-ast-analyzer` followed by a one-liner description
- Quick Example section appears before Installation and shows the verified `orders[status = "active"].items.price` expression with correct output
- Installation section shows pnpm/npm/yarn commands with ESM-only blockquote notice
- Five empty headings exist for Phases 19-22 (API Reference, CLI Usage, Examples, How It Works, Limitations)
- License section says MIT
- package.json license field says MIT (matching LICENSE file)
- `pnpm test` passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/18-overview-and-installation/18-01-SUMMARY.md`
</output>
