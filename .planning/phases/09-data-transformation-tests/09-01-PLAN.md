---
phase: 09-data-transformation-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/data-transforms.test.ts
autonomous: true
requirements:
  - TRFM-01
  - TRFM-02
  - TRFM-03
  - TRFM-04
  - TRFM-05

must_haves:
  truths:
    - "Pipeline chain expressions (filter, sort, map, reshape) have passing integration tests exercising path extraction"
    - "Apply operator (~>) expressions with HOF and lambda threading have passing integration tests"
    - "Array dot-notation mapping expressions produce correctly prefixed leaf paths in tests"
    - "String concatenation and formatting expressions produce both operand paths in tests"
    - "Multi-stage transforms with intermediate variable bindings resolve all hops in tests"
    - "Known analyzer bugs are documented as it.skip tests with tracking comments for v1.2"
    - "All tests use assertFixture() with exact match mode and explicit confidence in every PathResult"
  artifacts:
    - path: "test/integration/data-transforms.test.ts"
      provides: "All TRFM-01 through TRFM-05 integration test fixtures"
      contains: "TRFM-01|TRFM-02|TRFM-03|TRFM-04|TRFM-05"
      min_lines: 150
  key_links:
    - from: "test/integration/data-transforms.test.ts"
      to: "test/integration/helpers.ts"
      via: "import { assertFixture } and IntegrationFixture"
      pattern: "import.*assertFixture.*helpers"
    - from: "test/integration/data-transforms.test.ts"
      to: "src/index.ts"
      via: "assertFixture calls extractPaths internally"
      pattern: "assertFixture"
---

<objective>
Write integration test fixtures for all five TRFM requirements (TRFM-01 through TRFM-05) in the existing `data-transforms.test.ts` file, replacing the Phase 8 smoke test with real-world data transformation scenarios.

Purpose: Validate that the path analyzer correctly extracts paths from the most common JSONata production patterns -- pipeline chains, apply operators, array mapping, string formatting, and multi-stage variable transforms. Document known bugs via `it.skip` for v1.2.

Output: A fully populated `test/integration/data-transforms.test.ts` with 15-25 fixtures organized into 5 nested describe blocks, all using `assertFixture()` with exact match and explicit confidence.
</objective>

<execution_context>
@/home/simon-mainframe/.claude/get-shit-done/workflows/execute-plan.md
@/home/simon-mainframe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-transformation-tests/09-CONTEXT.md
@.planning/phases/09-data-transformation-tests/09-RESEARCH.md
@.planning/phases/08-test-infrastructure/08-01-SUMMARY.md

@test/integration/helpers.ts
@test/integration/data-transforms.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From src/types.ts:
```typescript
export type Confidence = "static" | "dynamic" | "partial";
export interface PathResult {
  path: string;
  confidence: Confidence;
}
```

From src/index.ts:
```typescript
export function extractPaths(expression: string): PathResult[];
export type { PathResult } from "./types.js";
export type { Confidence } from "./types.js";
```

From test/integration/helpers.ts:
```typescript
interface ExactFixture extends FixtureBase {
  expectedPaths: PathResult[];
  mustContain?: never;
  mustNotContain?: never;
}
export type IntegrationFixture = ExactFixture | SubsetFixture;
export function sortPaths(paths: PathResult[]): PathResult[];
export function assertFixture(fixture: IntegrationFixture): void;
```
</interfaces>

<verified_expressions>
<!-- CRITICAL: These expression -> output mappings were verified by running extractPaths() live during research.
     The executor MUST still pre-check each expression before writing the fixture (per CONTEXT.md decision),
     but these serve as known-good starting points. -->

TRFM-01 (passing):
  - `items[status = "active"]^(price).name` => items.name, items.price, items.status (all static)
  - `$map(items, function($v) { {"n": $v.name, "t": $v.price * $v.qty} })` => items, items.name, items.price, items.qty (all static)

TRFM-01 (skip - Bug A/B):
  - `$map(items[active], function($v) { $v.name })` => Bug A: produces spurious items.active.name
  - `($x := items; $x^(price))` => Bug B: missing items.price

TRFM-02 (passing):
  - `items ~> $map(function($v) { $v.price })` => items, items.price (all static)
  - `($fn := function($x) { $x.name }; data ~> $fn())` => data, data.name (all static)
  - `data ~> $map(function($v) { $v.value }) ~> $sum()` => data, data.value (all static)

TRFM-02 (skip - Bug C/D):
  - `items ~> $filter(fn{active}) ~> $map(fn{name})` => Bug C: predicate inheritance
  - `data ~> function($d) { $d.count }` => Bug D: inline lambda loses binding

TRFM-03 (all passing):
  - `orders.items.price` => orders.items.price (static)
  - `orders.items[active].price` => orders.items.price, orders.items.active (all static)

TRFM-04 (all passing):
  - `firstName & " " & lastName` => firstName, lastName (all static)
  - `address.city & ", " & address.state & " " & address.zip` => address.city, address.state, address.zip (all static)
  - `$join([firstName, " ", lastName])` => firstName, lastName (all static)
  - `$map(contacts, function($c) { $c.first & " " & $c.last })` => contacts, contacts.first, contacts.last (all static)

TRFM-05 (passing):
  - `($base := account; ...)` binding chain => account, account.firstName, account.lastName, account.department.name, account.id (all static)
  - `($raw := source.data; $mapped := $map($raw, fn{value}); $sum($mapped))` => source.data, source.data.value (all static)

TRFM-05 (skip - Bug A/C):
  - Complex chains with $filter then $map through variables => predicate path leaking
</verified_expressions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write TRFM-01 and TRFM-02 fixtures (pipeline chains and apply operators)</name>
  <files>test/integration/data-transforms.test.ts</files>
  <action>
Replace the entire contents of `data-transforms.test.ts` with the new test structure. Keep the existing imports (`describe`, `it` from vitest; `assertFixture` and `IntegrationFixture` from helpers). Structure the file as:

```typescript
describe("Data Transforms", () => {
  describe("TRFM-01: Pipeline chains (filter -> sort -> map -> reshape)", () => { ... });
  describe("TRFM-02: Chained ~> apply operator pipelines with lambda threading", () => { ... });
  // TRFM-03, 04, 05 placeholder describes -- Task 2 fills these
});
```

**TRFM-01 fixtures (3-4 passing + 1-2 skipped):**

Passing fixtures -- use expressions verified in research that avoid Bug A:
1. Filter-sort pipeline: `items[status = "active"]^(price).name` -- exercises filter predicate + sort key + field selection
2. Map with reshape: `$map(items, function($v) { {"n": $v.name, "t": $v.price * $v.qty} })` -- exercises HOF map with object constructor
3. Filter-then-select (no HOF): `orders[type = "wholesale"].(quantity * unitPrice)` -- exercises filter + calculated field (pre-check this expression first!)
4. Composite pipeline: a multi-step expression combining filter + sort + field selection without HOF data arg bug (pre-check first!)

Skipped fixtures -- document known bugs:
1. `it.skip` for Bug A: `$map(items[active], function($v) { $v.name })` -- filter predicate leaks into HOF element binding. Comment: `// BUG(v1.2): filter predicate paths leak into HOF element bindings -- items[active] bound as element path to $v`
2. `it.skip` for Bug B: `($x := items; $x^(price))` -- variable-resolved sort path lost. Comment: `// BUG(v1.2): variable-resolved sort paths not extracted -- $x^(price) misses sort term`

Use this tracking comment format consistently: `// BUG(v1.2): <description>` on the line before `it.skip(...)`.

**TRFM-02 fixtures (3-4 passing + 1-2 skipped):**

Passing fixtures:
1. Single apply with HOF: `items ~> $map(function($v) { $v.price })` -- basic ~> piping
2. Apply with variable-bound lambda: `($fn := function($x) { $x.name }; data ~> $fn())` -- custom function via variable workaround
3. Chained ~> without filter: `data ~> $map(function($v) { $v.value }) ~> $sum()` -- clean chain (no bug since no filter)
4. Apply with $reduce or $each (pre-check first!) -- exercise a different HOF through ~>

Skipped fixtures:
1. `it.skip` for Bug C: chained `~>` filter then map -- `items ~> $filter(function($v) { $v.active }) ~> $map(function($v) { $v.name })`. Comment: `// BUG(v1.2): chained ~> HOF inherits filter predicate paths from prior stage`
2. `it.skip` for Bug D: inline lambda with ~> -- `data ~> function($d) { $d.count }`. Comment: `// BUG(v1.2): inline lambda with ~> apply loses parameter binding -- $d never bound to data paths`

**Pattern rules (from CONTEXT.md decisions):**
- Use `IntegrationFixture[]` array + `for...of` loop + `assertFixture()` for passing fixtures
- Use standalone `it.skip(name, () => { assertFixture({...}) })` for skipped fixtures, OUTSIDE the fixture array
- Multi-line template literals for complex expressions, single-line strings for simple ones
- Realistic variable names: `$filtered`, `$sorted`, `$mapped`, `$item`, `$employee`
- Fixture names combine pattern and behavior: `"filter-sort pipeline: extracts filter predicate and sort key paths"`
- Confidence level explicit in EVERY PathResult

**CRITICAL: Pre-check every expression through extractPaths() before writing the fixture.** The research provides verified patterns but the executor MUST still confirm the actual output for any new expressions not listed in the verified_expressions block above. Run:
```typescript
import { extractPaths } from "../../src/index.js";
console.log(JSON.stringify(extractPaths('expression here'), null, 2));
```
Use `npx tsx` or `node --loader ts-node/esm` to run quick checks against the built dist, or use `npx vitest run test/integration/data-transforms.test.ts` to validate iteratively.

Leave empty `describe` blocks for TRFM-03, TRFM-04, TRFM-05 as placeholders for Task 2.
  </action>
  <verify>
    <automated>npx vitest run test/integration/data-transforms.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - TRFM-01 describe block has 3+ passing fixtures and 1-2 it.skip fixtures with BUG(v1.2) tracking comments
    - TRFM-02 describe block has 3+ passing fixtures and 1-2 it.skip fixtures with BUG(v1.2) tracking comments
    - All passing tests use assertFixture() with exact match (expectedPaths) and explicit confidence
    - All skipped tests have consistent `// BUG(v1.2): ...` tracking comments
    - `npx vitest run test/integration/data-transforms.test.ts` passes (skipped tests reported but not failures)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write TRFM-03, TRFM-04, TRFM-05 fixtures (array mapping, string formatting, multi-stage transforms)</name>
  <files>test/integration/data-transforms.test.ts</files>
  <action>
Fill in the TRFM-03, TRFM-04, and TRFM-05 describe blocks that Task 1 left as placeholders.

**TRFM-03 fixtures (3-4 passing, no skips expected -- research found no bugs):**

1. Simple dot-notation: `orders.items.price` -- single leaf through nested arrays
2. Dot-notation with filter: `orders.items[active].price` -- filter predicate + context-relative field
3. Multi-level dot-notation: something like `company.departments.employees.name` -- 3+ levels of nesting (pre-check first!)
4. Dot-notation with multiple fields: expression that selects multiple leaf fields through array context, e.g., `orders.items.(name & " - " & $string(price))` or an object constructor (pre-check first!)

**TRFM-04 fixtures (3-5 passing, no skips expected -- research found no bugs):**

1. Simple concatenation: `firstName & " " & lastName` -- two paths with string literal separator
2. Multi-field address formatting: `address.city & ", " & address.state & " " & address.zip` -- 3 nested paths concatenated
3. $join with array constructor: `$join([firstName, " ", lastName])` -- built-in function with mixed path/literal args
4. Map with string concat in lambda: `$map(contacts, function($c) { $c.first & " " & $c.last })` -- HOF + concatenation
5. (Optional) A more complex string template, e.g., using `$string()` for type coercion with numeric fields (pre-check first!)

**TRFM-05 fixtures (2-3 passing + 1-2 skipped):**

Passing fixtures:
1. Simple variable binding chain: `($base := account; $name := $base.firstName & " " & $base.lastName; {"display": $name, "dept": $base.department.name, "id": $base.id})` -- multi-hop variable resolution
2. Multi-hop via $map then $sum: `($raw := source.data; $mapped := $map($raw, function($v) { $v.value }); $sum($mapped))` -- variable -> HOF -> aggregation chain
3. (Optional) Another clean multi-stage pattern with realistic variable names (pre-check first!)

Skipped fixtures:
1. `it.skip` for complex chain with $filter then $map through variables (Bug A/C). Comment: `// BUG(v1.2): filter predicate paths leak through variable-bound intermediate results`
   Expression example: `($data := items[active]; $map($data, function($v) { $v.name }))` (pre-check to confirm it hits the bug)

**After filling all five requirement blocks, add one composite fixture** at the end of the Data Transforms describe (after TRFM-05), in its own describe block:

```typescript
describe("Composite: cross-pattern data transform", () => {
  // A single realistic expression combining patterns from multiple TRFM requirements
  // that the analyzer handles correctly (no known bugs)
  // e.g., dot-notation mapping + string formatting + variable binding
});
```

Choose a composite expression that combines at least 2-3 TRFM patterns (dot-notation + string concat + variable binding is a good candidate since those are all bug-free). Pre-check the expression before writing the fixture.

**Same pattern rules as Task 1:** IntegrationFixture[] array, for...of loop, assertFixture(), realistic names, explicit confidence, BUG(v1.2) tracking comments.

**CRITICAL: Pre-check every expression.** Any expression not in the verified_expressions list MUST be tested via extractPaths() before being written as a fixture. Use the iterative approach: write fixture, run test, fix expected output if needed.
  </action>
  <verify>
    <automated>npx vitest run test/integration/data-transforms.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    - TRFM-03 describe block has 3+ passing fixtures covering dot-notation array mapping
    - TRFM-04 describe block has 3+ passing fixtures covering string concatenation/formatting
    - TRFM-05 describe block has 2+ passing fixtures and 1+ it.skip fixture with BUG(v1.2) comment
    - Composite describe block has 1 passing fixture combining multiple TRFM patterns
    - All passing tests use assertFixture() with exact match and explicit confidence
    - `npx vitest run test/integration/data-transforms.test.ts` passes with 0 failures
    - `npm run test:integration` passes (all integration test files still work)
    - Total passing fixture count is 15+ across all TRFM blocks
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run test/integration/data-transforms.test.ts` -- all non-skipped tests pass, skipped tests reported
2. `npm run test:integration` -- full integration suite passes (including other category file smoke tests)
3. `npm run test:unit` -- existing unit tests unaffected
4. `npm test` -- full suite passes
5. Grep for describe blocks: every TRFM-01 through TRFM-05 has its own describe block
6. Grep for `BUG(v1.2)`: all skipped tests have consistent tracking comments
7. Grep for `it.skip`: all skipped tests still contain assertFixture call with expectedPaths showing the CORRECT expected output (not the buggy output)
8. No `mustContain` usage anywhere -- exact match only per CONTEXT.md decision
</verification>

<success_criteria>
- test/integration/data-transforms.test.ts contains 5 nested describe blocks (TRFM-01 through TRFM-05) plus a composite describe
- 15+ passing test fixtures across all blocks
- 3-5 skipped test fixtures documenting known analyzer bugs with BUG(v1.2) tracking comments
- Every fixture uses assertFixture() with exact match (expectedPaths) and explicit confidence in every PathResult
- `npm test` passes with zero failures (skipped tests do not count as failures)
- No subset matching (mustContain/mustNotContain) used anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/09-data-transformation-tests/09-01-SUMMARY.md`
</output>
