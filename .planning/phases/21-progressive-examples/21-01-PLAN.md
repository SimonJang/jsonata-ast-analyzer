---
phase: 21-progressive-examples
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - EXMP-01

must_haves:
  truths:
    - "README Examples section contains 5 worked examples progressing from simple to complex"
    - "Each example shows a JSONata expression and the full extractPaths output with confidence annotations"
    - "All three confidence levels (static, dynamic, partial) appear across the example set"
    - "No example reuses an expression already in the README (Quick Example, CLI, confidence table)"
  artifacts:
    - path: "README.md"
      provides: "Progressive examples section content"
      contains: "customer.name"
  key_links:
    - from: "README.md Examples section"
      to: "README.md API Reference confidence table"
      via: "Examples demonstrate the confidence levels documented in the table"
      pattern: "static.*dynamic.*partial"
---

<objective>
Write 5 progressive worked examples under the existing `## Examples` heading in README.md, demonstrating all three confidence levels across increasingly complex JSONata patterns.

Purpose: A developer reading the README can see how the analyzer handles real-world expressions of increasing complexity, building intuition for what extractPaths returns and when each confidence level appears.
Output: README.md with populated Examples section (5 examples, all 3 confidence levels covered).
</objective>

<execution_context>
@/home/simon-mainframe/.claude/get-shit-done/workflows/execute-plan.md
@/home/simon-mainframe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-progressive-examples/21-RESEARCH.md

# Prior phase summaries (Phase 18 created the skeleton, Phase 19 documented confidence levels)
@.planning/phases/18-overview-and-installation/18-01-SUMMARY.md
@.planning/phases/19-api-reference/19-01-SUMMARY.md

<interfaces>
<!-- Current README structure -- insertion point is between line 105 (## Examples) and line 107 (## How It Works) -->
<!-- The ## Examples heading already exists (empty). Insert content AFTER this heading and BEFORE ## How It Works. -->
<!-- Do NOT modify any content outside the Examples section. -->

Expressions already in README (DO NOT REUSE):
- orders[status = "active"].items.price (Quick Example)
- account.name (CLI argument mode + confidence table)
- $sum(orders.total) (CLI stdin mode)
- item[$field] (Confidence table)
- orders.items.%.orderRef (Confidence table)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write progressive examples content in README.md</name>
  <files>README.md</files>
  <action>
Insert content under the existing `## Examples` heading (line 105) in README.md, before the `## How It Works` heading (line 107). Do NOT modify any content outside this range.

Before writing, verify each example's output by running the expression against the built CLI:
```
node dist/cli.js 'customer.name'
node dist/cli.js '($addr := customer.address; $addr.city & ", " & $addr.state)'
node dist/cli.js 'products[price > 50 and inStock].name'
node dist/cli.js 'inventory[$category].quantity'
node dist/cli.js 'orders.items.{"itemName": name, "orderDate": %.date}'
```

Write 5 examples using H3 subheadings, each with:
1. A brief (1 sentence) description of what the expression demonstrates
2. A JavaScript code block showing `extractPaths()` call and output as comments (matching the Quick Example format from Phase 18)

The 5 examples in order:

**### Simple property access**
Expression: `customer.name`
Shows: baseline single dot-path, one static result.

**### Variable assignment**
Expression: `($addr := customer.address; $addr.city & ", " & $addr.state)`
Shows: analyzer traces through variable binding to find real source paths. All static.

**### Filter predicates**
Expression: `products[price > 50 and inStock].name`
Shows: analyzer discovers paths hidden inside filter conditions (price, inStock). A developer might only think about products.name -- the analyzer reveals all three.

**### Dynamic computed path**
Expression: `inventory[$category].quantity`
Shows: first appearance of `dynamic` confidence. Unbound variable in bracket position produces `[*]` wildcard marker.

**### Parent operator**
Expression: `orders.items.{"itemName": name, "orderDate": %.date}`
Shows: `partial` confidence from parent operator (`%`). Mixed confidence in a single result set.

Use the verified output from RESEARCH.md for each example. Copy the exact JSON output confirmed by CLI verification -- do not hand-type path results. Use the JS import comment style matching the Quick Example section (not the CLI shell command style).

Style notes:
- Keep prose minimal -- one sentence per example, let the code block do the work
- Match the clean, no-badges, no-TOC style established in Phases 18-20
- Use em-dash style consistent with rest of README
  </action>
  <verify>
    <automated>node dist/cli.js 'customer.name' && node dist/cli.js '($addr := customer.address; $addr.city & ", " & $addr.state)' && node dist/cli.js 'products[price > 50 and inStock].name' && node dist/cli.js 'inventory[$category].quantity' && node dist/cli.js 'orders.items.{"itemName": name, "orderDate": %.date}' && pnpm test</automated>
  </verify>
  <done>
README.md Examples section contains 5 progressive examples:
- Example 1 (customer.name): 1 static path
- Example 2 (variable tracing): 3 static paths
- Example 3 (filter predicates): 3 static paths
- Example 4 (dynamic computed): 1 static + 1 dynamic path
- Example 5 (parent operator): 2 static + 1 partial path
All three confidence levels appear. No expressions duplicated from other README sections. Full test suite passes (no code changes, just docs).
  </done>
</task>

</tasks>

<verification>
1. README.md Examples section has 5 H3 sub-headings with JS code blocks
2. Each code block shows extractPaths() call with comment output
3. Confidence levels present: grep for "static", "dynamic", and "partial" in Examples section
4. No expression overlap: none of the 5 avoided expressions appear in Examples section
5. Full test suite passes: `pnpm test` (294 tests, no code changes)
6. No content outside Examples section was modified
</verification>

<success_criteria>
- README Examples section populated with 5 progressive worked examples
- All three confidence levels (static, dynamic, partial) represented
- Every example output verified against actual CLI output
- Clean, minimal prose matching Phase 18-20 documentation style
- Test suite green (294 tests)
</success_criteria>

<output>
After completion, create `.planning/phases/21-progressive-examples/21-01-SUMMARY.md`
</output>
