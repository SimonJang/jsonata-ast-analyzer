---
phase: 11-api-reshaping-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [test/integration/api-reshaping.test.ts]
autonomous: true
requirements: [APIR-01, APIR-02, APIR-03, APIR-04, APIR-05]

must_haves:
  truths:
    - "Nested API payload extraction with flattening produces all leaf paths from the source structure"
    - "Object constructors pulling from multiple root-level paths produce paths under each distinct root"
    - "Deep path traversal through 5-6 levels produces correct single leaf path"
    - "Context variable binding with cross-reference resolves paths through the binding correctly (clean patterns pass, buggy patterns documented as it.skip)"
    - "Parent operator produces partial-confidence paths with % segment (flat paths pass, object-constructor-step patterns documented as it.skip)"
    - "Composite fixture combining bug-free APIR-01/02/03 patterns passes without skip"
    - "All passing fixtures use exact match (expectedPaths) with explicit confidence"
  artifacts:
    - path: "test/integration/api-reshaping.test.ts"
      provides: "APIR-01 through APIR-05 integration test fixtures plus composite"
      min_lines: 200
      contains: "describe.*APIR-0[1-5]"
  key_links:
    - from: "test/integration/api-reshaping.test.ts"
      to: "test/integration/helpers.ts"
      via: "import { assertFixture } and IntegrationFixture type"
      pattern: "import.*assertFixture.*helpers"
    - from: "test/integration/api-reshaping.test.ts"
      to: "src/index.ts"
      via: "assertFixture calls extractPaths internally"
      pattern: "assertFixture\\(fixture\\)"
---

<objective>
Write integration tests validating path extraction from API payload reshaping patterns -- nested extraction, mixed sources, deep traversal, context variables, and parent operators.

Purpose: Validate APIR-01 through APIR-05 against the live analyzer, documenting bugs as it.skip with BUG(v1.2) tracking comments. This is the third of five test category phases.
Output: Populated `test/integration/api-reshaping.test.ts` with ~16-21 fixtures (12-16 passing, 4-6 skipped)
</objective>

<execution_context>
@/home/simon-mainframe/.claude/get-shit-done/workflows/execute-plan.md
@/home/simon-mainframe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-api-reshaping-tests/11-RESEARCH.md

@test/integration/helpers.ts
@test/integration/business-rules.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From test/integration/helpers.ts:
```typescript
export type IntegrationFixture = ExactFixture | SubsetFixture;
// Only ExactFixture mode used in this phase:
// { name: string; expression: string; expectedPaths: PathResult[] }

export function sortPaths(paths: PathResult[]): PathResult[];
export function assertFixture(fixture: IntegrationFixture): void;
```

From src/index.ts:
```typescript
export function extractPaths(expression: string): PathResult[];
export interface PathResult { path: string; confidence: "static" | "partial" }
```

Import pattern (from business-rules.test.ts):
```typescript
import { describe, it } from "vitest";
import { assertFixture } from "./helpers.js";
import type { IntegrationFixture } from "./helpers.js";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: APIR-01, APIR-02, APIR-03 fixtures (all passing)</name>
  <files>test/integration/api-reshaping.test.ts</files>
  <action>
Replace the skeleton `api-reshaping.test.ts` with the full test file structure. Add imports (`describe`, `it` from vitest; `assertFixture` and `IntegrationFixture` from `./helpers.js`). Create the top-level `describe("API Reshaping", () => { ... })` block.

Add three nested describe blocks for the bug-free requirements:

**APIR-01: Nested API payload extraction + flattening (3 fixtures)**
All verified working. Use the exact expressions and expectedPaths from RESEARCH.md Code Examples:
1. `"nested API payload extraction: extracts all leaf paths from deep source structure"` -- direct nested object constructor pulling from `response.data.user.*` (3 paths)
2. `"variable-bound API extraction: resolves variable through nested source paths"` -- `$user := response.data.user` then construct object from `$user.profile.*` and `$user.contact.*` (4 paths)
3. `"map over nested API records: extracts base array and deep leaf paths from lambda"` -- `$map(response.data.records, function($r) {...})` (3 paths)

**APIR-02: Mixed sources with multiple root paths (3 fixtures)**
All verified working. Use expressions from RESEARCH.md:
1. `"mixed source object: extracts paths under each distinct root"` -- object constructor with `account.profile.name`, `$count(orders)`, `billing.currentBalance` (3 paths)
2. `"multi-variable mixed sources: resolves variables from independent roots into single output"` -- `$acct := account; $ship := shipping` then construct (5 paths)
3. `"array index with mixed roots: extracts paths from multiple roots with index access"` -- `orders[0].id` strips to `orders.id` (3 paths)

**APIR-03: Deep path traversal with array flattening (3 fixtures)**
All verified working. Use expressions from RESEARCH.md:
1. `"deep path traversal: extracts single leaf path through 6-level nesting"` -- `api.response.data.records.fields.value` (1 path)
2. `"deep traversal with filter: extracts leaf path and filter predicate through nested arrays"` -- `response.data.items[active].attributes.dimensions.height` (2 paths)
3. `"map over deep nested structure: extracts base array and deep leaf path from lambda"` -- `$map(response.data.items, function($item) {...})` (2 paths)

Each describe block uses the established pattern: typed `fixtures: IntegrationFixture[]` array, then `for (const fixture of fixtures) { it(fixture.name, () => { assertFixture(fixture); }); }`.

Run `npx vitest run test/integration/api-reshaping.test.ts` to verify all 9 fixtures pass.
  </action>
  <verify>
    <automated>npx vitest run test/integration/api-reshaping.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>9 passing fixtures for APIR-01 (3), APIR-02 (3), APIR-03 (3) -- all green, no skips</done>
</task>

<task type="auto">
  <name>Task 2: APIR-04, APIR-05, and Composite fixtures</name>
  <files>test/integration/api-reshaping.test.ts</files>
  <action>
Add three more nested describe blocks to the existing file:

**APIR-04: Context variable binding with cross-reference (2 passing + 2 skipped)**
Passing fixtures (use expressions from RESEARCH.md):
1. `"variable-bound API payload: resolves variable through nested API response paths"` -- `$payload := event.data` then construct object (4 paths). Clean: variable used in object construction, NOT in filter.
2. `"variable cross-reference in calculation: resolves bound variable in arithmetic context"` -- `$config := settings; order.amount * $config.taxRate` (3 paths). Clean: variable used in arithmetic, NOT in filter.

Skipped fixtures (use `it.skip` with `BUG(v1.2):` comment on line before):
3. Focus variable double-prefix bug: `"focus variable cross-reference: extracts focus-resolved paths without double prefix"` -- `orders@$o[$o.total > 100].id`. Expected CORRECT output: `[orders.id (static), orders.total (static)]`. Bug comment: `// BUG(v1.2): focus variable @$o double-prefixes -- $o resolves to ["orders"], then filter context re-prefixes to "orders.orders.total"`
4. Variable in filter predicate leak: `"variable cross-reference in filter: extracts variable source and filter paths without spurious prefixing"` -- `($cfg := config; items[$cfg.minPrice < price].name)`. Expected CORRECT output: `[config (static), items.name (static), items.price (static)]`. Bug comment: `// BUG(v1.2): variable-resolved paths in filter predicates get spuriously context-prefixed`

Passing fixtures use the `fixtures: IntegrationFixture[]` array + `for` loop pattern. Skipped fixtures are standalone `it.skip(...)` outside the array, matching Phase 9/10 convention.

**APIR-05: Parent operator in nested mapped contexts (2 passing + 2 skipped)**
Passing fixtures:
1. `"parent operator in flat path: produces partial-confidence path with % segment"` -- `orders.items.%.orderRef` (1 path, confidence: "partial")
2. `"two-level parent operator: produces path with double % segments"` -- `company.departments.employees.%.%.companyName` (1 path, confidence: "partial")

Skipped fixtures:
3. Parent in object constructor step: `"parent in object constructor: extracts both local and parent-scoped paths"` -- `orders.items.{"itemName": name, "orderDate": %.date}`. Expected CORRECT output: `[orders.items (static), orders.items.%.date (partial), orders.items.name (static)]`. Bug comment: `// BUG(v1.2): walkPath does not walk value expressions inside unary (object constructor) path steps -- all inner paths silently dropped`
4. Parent in block path step: `"parent in block path step: extracts expression paths from block within path"` -- `orders.items.(%.orderRef & ": " & name)`. Expected CORRECT output: `[orders.items.%.orderRef (partial), orders.items.name (static)]`. Bug comment: `// BUG(v1.2): walkPath does not walk inner expressions inside block path steps -- same root cause as object constructor step`

**Composite: Cross-pattern API reshaping (1 passing fixture)**
Combine ONLY bug-free patterns from APIR-01/02/03 in a single realistic expression. For example:
```
($resp := api.response;
 $items := $resp.data.items;
 {
   "user": $resp.meta.requestedBy,
   "count": $count($items),
   "topItem": $map($items[featured], function($v) {
     {"name": $v.title, "category": $v.attributes.category}
   }),
   "source": config.apiVersion
 })
```
Run this expression through `extractPaths()` first to get actual output, then codify the actual correct result as `expectedPaths`. The composite must NOT use `@$v` focus variables, variables inside filter predicates, or `.{}` / `.()` path steps (those are buggy).

The fixture name should follow: `"combined patterns: resolves all paths across APIR-01/02/03"`.

Run `npx vitest run test/integration/api-reshaping.test.ts` to verify all passing fixtures pass and skipped fixtures are skipped.
  </action>
  <verify>
    <automated>npx vitest run test/integration/api-reshaping.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>14+ passing fixtures, 4 skipped fixtures. Full test suite (`npm run test:integration`) shows no regressions. All APIR-01 through APIR-05 requirements have coverage via at least one fixture each.</done>
</task>

</tasks>

<verification>
1. `npx vitest run test/integration/api-reshaping.test.ts` -- all non-skipped fixtures pass, skipped count matches expected (4)
2. `npm run test:integration` -- full integration suite passes (no regressions in data-transforms or business-rules)
3. `npm test` -- full project suite passes (unit + integration)
4. Every APIR requirement has at least one dedicated `describe("APIR-XX: ...")` block
5. All `it.skip` fixtures have `BUG(v1.2):` tracking comment on the preceding line
6. All skipped fixtures show CORRECT expected output (not buggy actual)
7. Composite fixture combines only bug-free patterns and passes without skip
</verification>

<success_criteria>
- api-reshaping.test.ts contains 5 APIR describe blocks + 1 Composite describe block
- 12+ passing integration test fixtures
- 4 skipped fixtures with BUG(v1.2) tracking (2 for APIR-04 bugs, 2 for APIR-05 bugs)
- All fixtures use exact match (expectedPaths) with explicit confidence
- Composite fixture passes without skip, combining APIR-01/02/03 patterns
- Full test suite green: `npm test` passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/11-api-reshaping-tests/11-01-SUMMARY.md`
</output>
