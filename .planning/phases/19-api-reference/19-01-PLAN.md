---
phase: 19-api-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [API-01, API-02, API-03]

must_haves:
  truths:
    - "README documents extractPaths with its TypeScript signature, parameter, return type, and error behavior"
    - "README shows the PathResult interface with path and confidence fields"
    - "README includes a confidence level table with static, dynamic, and partial rows"
  artifacts:
    - path: "README.md"
      provides: "API Reference section content"
      contains: "### extractPaths(expression: string): PathResult[]"
  key_links:
    - from: "README.md ## API Reference"
      to: "README.md ## CLI Usage"
      via: "Content inserted between these two existing headings"
      pattern: "### extractPaths.*### Types.*## CLI Usage"
---

<objective>
Write the API Reference section of README.md documenting extractPaths, PathResult, and Confidence.

Purpose: A developer landing on the README can understand the complete programmatic API (one function, two types) without reading source code.
Output: README.md with populated API Reference section between the existing heading and CLI Usage heading.
</objective>

<execution_context>
@/home/simon-mainframe/.claude/get-shit-done/workflows/execute-plan.md
@/home/simon-mainframe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-api-reference/19-CONTEXT.md
@.planning/phases/19-api-reference/19-RESEARCH.md
@.planning/phases/18-overview-and-installation/18-01-SUMMARY.md
@README.md

<interfaces>
<!-- Source types and signatures the executor needs to document accurately -->

From src/index.ts:
```typescript
export function extractPaths(expression: string): PathResult[]
```
- Parameter: expression: string -- a JSONata expression string
- Returns: Deduplicated array of PathResult objects with confidence annotations
- Throws: On invalid JSONata input (parser error propagates unmodified)
- Empty string throws: "Unexpected end of expression"
- Deduplication: new Set(rawPaths) ensures each unique path appears once

From src/types.ts:
```typescript
export type Confidence = "static" | "dynamic" | "partial";

export interface PathResult {
  path: string;
  confidence: Confidence;
}
```

Confidence classification (from deriveConfidence in src/index.ts):
- "partial": path contains "%" as a whole dot-separated segment (parent operator) -- checked first
- "dynamic": path contains "[*]" anywhere (unresolvable bracket-filter variable) -- checked second
- "static": all other paths including explicit wildcards like * and ** -- default
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write API Reference content in README.md</name>
  <files>README.md</files>
  <action>
Insert content between the existing `## API Reference` heading (line 33) and `## CLI Usage` heading (line 35) in README.md. Do NOT duplicate the `## API Reference` H2 heading -- it already exists.

The content has two H3 subsections per CONTEXT.md locked decisions:

**Subsection 1: `### extractPaths(expression: string): PathResult[]`**

- Brief description of what the function does (extracts all data paths a JSONata expression reads from its input)
- Bullet list with:
  - **expression** parameter: a JSONata expression string
  - **Returns**: deduplicated array of PathResult objects. Each unique path appears once.
  - **Throws**: on invalid JSONata expressions; parser error propagates unmodified. Empty string throws "Unexpected end of expression".
- A note that extractPaths always returns an array (never null) or throws -- for JS users who don't read types
- NO usage example here (the Quick Example section above already demonstrates it -- locked decision)

**Subsection 2: `### Types`**

- TypeScript code block showing PathResult interface and Confidence type WITHOUT `export` keywords or JSDoc comments. Present PathResult first, then Confidence:
  ```typescript
  interface PathResult {
    path: string;
    confidence: Confidence;
  }

  type Confidence = "static" | "dynamic" | "partial";
  ```
- Brief bullet list below the code block describing each PathResult field:
  - **path**: the dot-separated data path. Special markers: `[*]` indicates a dynamic segment, `%` indicates a parent reference.
  - **confidence**: how certain the analysis is about this path. See table below.
- One-liner intro explaining priority order: when a path matches multiple levels, the highest priority wins: partial > dynamic > static
- 4-column confidence table (Level, Meaning, Cause, Example):
  - `static` | fully resolved at analysis time | all path segments are known | `account.name`
  - `dynamic` | contains unresolvable segments | variable used in bracket filter position | `item[$field]`
  - `partial` | contains parent operator approximation | parent operator (%) in path | `orders.items.%.orderRef`
- The Example column shows expression-only (no output column -- locked decision; Phase 21 handles full examples)

**Critical constraints:**
- Do NOT add `export` keywords to type definitions in the code block
- Do NOT add a usage example in the extractPaths subsection
- Do NOT add an output column to the confidence table
- Do NOT duplicate the `## API Reference` H2 heading
- Preserve the `## CLI Usage` heading exactly as-is after the new content
  </action>
  <verify>
    <automated>grep -c "### extractPaths(expression: string): PathResult" README.md && grep -c "### Types" README.md && grep -c "interface PathResult" README.md && grep -c "type Confidence" README.md && grep -c "static" README.md && grep -c "dynamic" README.md && grep -c "partial" README.md && grep "## CLI Usage" README.md | head -1</automated>
  </verify>
  <done>
- README.md contains `### extractPaths(expression: string): PathResult[]` subsection with parameter, return, throws, and deduplication documentation
- README.md contains `### Types` subsection with clean PathResult interface and Confidence type code block
- README.md contains 4-column confidence table with static, dynamic, and partial rows
- `## CLI Usage` heading still exists immediately after the API Reference content
- No `export` keywords in type definition code block
- No usage example in extractPaths subsection
  </done>
</task>

</tasks>

<verification>
Structural checks (all must pass):
1. `grep -c "### extractPaths" README.md` returns 1
2. `grep -c "### Types" README.md` returns 1
3. `grep -c "## API Reference" README.md` returns exactly 1 (not duplicated)
4. `grep -c "## CLI Usage" README.md` returns exactly 1 (preserved)
5. `grep "export" README.md` does NOT match inside the Types code block
6. README.md renders valid markdown (no unclosed code fences)

Content accuracy checks:
7. extractPaths subsection mentions: parameter (expression: string), return type (PathResult[]), throws behavior, empty string error, deduplication, always returns array or throws
8. Types subsection shows PathResult with path and confidence fields
9. Confidence table has 3 data rows (static, dynamic, partial) with 4 columns (Level, Meaning, Cause, Example)
10. Priority order mentioned: partial > dynamic > static
</verification>

<success_criteria>
A developer reading only the README can answer: What function do I call? What does it accept? What does it return? What can go wrong? What do the types look like? What do the confidence levels mean?
</success_criteria>

<output>
After completion, create `.planning/phases/19-api-reference/19-01-SUMMARY.md`
</output>
